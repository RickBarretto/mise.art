[env]
MISE_USE_VERSIONS_HOST = "0"

[tasks. test]
description = "Run plugin tests - install and execute tool"
run = "sh mise-tasks/test"

[tasks.ci]
description = "Run all CI checks"
depends = ["test"]
